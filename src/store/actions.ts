import * as auth from './auth/action';
import * as proxy from './proxy/action';
import * as command from './command/action';
import * as request from './request/action';

export default {
  auth,
  proxy,
  command,
  request,
};
