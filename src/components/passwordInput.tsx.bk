import * as React from 'react';
import {
  Input,
} from 'antd';

class PasswordInput extends React.Component {
  render() {
    return (
      <Input {...this.props} />
    );
  }
}

export default PasswordInput;
